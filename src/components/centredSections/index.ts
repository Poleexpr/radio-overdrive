import { ProgramCentredSection } from './ProgramCentredSection';
import { TeamCentredSection } from './TeamCentredSection';

export const CentredSections = {
  ProgramCentredSection,
  TeamCentredSection,
};
